@model MSACSDegreePlanner.Models.Degree

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Degree</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.DegreeAbbrev) </dt>
        <dd>@Html.DisplayFor(model => model.DegreeAbbrev) </dd>
        <dt>@Html.DisplayNameFor(model => model.DegreePlanName) </dt>
        <dd>@Html.DisplayFor(model => model.DegreePlanName)</dd>
        <dt>@Html.DisplayNameFor(model => model.Requirements)  </dt>
        <dd>
            <table class="table">
                <tr>
                    <th>Number</th>
                    <th>Abbrev</th>
                    <th>Requirement Name</th>
                </tr>
                @foreach (var item in Model.Requirements.OrderBy(r => r.RequirementId))
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.RequirementId) </td>
                        <td>@Html.DisplayFor(modelItem => item.RequirementAbbrev) </td>
                        <td>@Html.DisplayFor(modelItem => item.RequirementName)   </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.DegreeId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
